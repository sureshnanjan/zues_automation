let objective = "This is a QA Training Programme"
typeof(objective)
console.log(objective)

/**
 * This is a sub heading
 * @param {number} param1 
 * @param {number} param2 
 * @returns{number}
 */

function MyMethod(param1,param2)
{

    
    return 100;
}
