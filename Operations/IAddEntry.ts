export interface IEnteryAdd{
    verifypopup(); //Verify that the pop-up opens on accessing the link
    verifyclosebutton(); //Verify that the pop-up closes by using close button
    verifyheading(); //Heading of the page
    verifycontent(); // Content of the page
    verifyclickhere(); //pop-up modal should be displayed again

}