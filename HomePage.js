let pageHeading = 'test'; //Heading of the page

function PageAccess(){
    //Access page page
    console.log("Test 1")
}

/**
 * Represents a book.
 * @constructor
 * @param {string} pageTitle - The title of the page.
 * @param {string} addElement - The add button on the page.
 */

function addOperation (pageTitle, addElement) {
    //Open page
    //Find Add button
    //Click on Add button
    console.log("Test 2")
}

PageAccess();
addOperation();
