let Heading = "";
let SubHeading = "";
/**
 * Access a page in the Heroku App
 * @param {string} name - Provide the name of the Page
 */
function AccessPages(name){
    // Using Plawright
    // Launch Browser 
    // Take the name of page and navigate to it
}

AccessPages("AddRemove")
let page = AddRemoveElements()