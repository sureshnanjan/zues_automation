import { Page, BrowserContext, test, expect } from '@playwright/test';
import { iFrames } from '../Project-3/iFrames'

test('Verify the no. of links available', async ({ page }) => {
    
})

test('Verify the 1st link is clickable', async ({ page }) => {
    
})

test('Verify the 2nd link is clickable', async ({ page }) => {
    
})

