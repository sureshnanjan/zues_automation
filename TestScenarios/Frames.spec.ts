import { Page, BrowserContext, test, expect } from '@playwright/test';
import { Frames } from '../PageObject/Frames'

// Test case 01: To verify link count in the page
test('Verify the no. of links available', async ({ page }) => {
    // Step 1: Launch the link
    // const HomePage = new iHomepage
    // const Frame = new iFramepage
    // Homepage.gotoexample{'Frames'}
    // Frame.checklinkcount(2)
})

//  Test case 02: To verify iFrames example is clickable
test('Verify the iFrame example is clickable', async ({ page }) => {
    // Step 1: Launch the link
    // const HomePage = new iHomepage
    // const Frame = new iFramepage
    // Homepage.gotoexample{'Frames'}
    // 
})

//  Test case 03: To verify Nested Frames example is clickable
test('Verify the Nested Frame is clickable', async ({ page }) => {
    
})

//  Test case 04: To verify user has navigated to the correct iFrame link
test('To check the iFrame link has correctly loaded or not', async ({ page }) => {
    
})

//  Test case 05: To verify user has navigated to the correct Nested Frame link
test('To check the Nested Frame link has correctly loaded or not', async ({ page }) => {
    
})


