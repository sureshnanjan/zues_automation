/**
 * projectName is the project name of the string type
 */
let projectName = "Petstore";
console.log(projectName)

/** 
 * color @type {string} 
 *length @type {number}
 *breadth @type {number}
 *font @type {string}
*/

const PetStoreLogo = {color:"green",length:20,breadth:10,font:"times"}
console.log(PetStoreLogo["color"]);
console.log(PetStoreLogo["length"]);
console.log(PetStoreLogo["breadth"]);
console.log(PetStoreLogo["font"]);

/**
 * myPets is an array of pet types of the string datatype
 */
const myPets = ['fish','dogs','cats'];
console.log(myPets.length);

