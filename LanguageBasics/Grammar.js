/**
 * let is one of the three types of variable declarations.
 * Here the value Razia is a string literal.
 */
let myName = 'Razia';
let myAge; // This is just a Declaration.
myAge = 26; // This is Initilization.

/**
 * constant is one of the three types of variable declarations
 * Here 3.14 is a numeric literal
 */
const PI = 3.14;

// constant myAddress; is incorrect, as const declarations always need an initializer

const myPetStoreLogo = {color:'red'}